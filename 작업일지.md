# PeopleFlow AI 작업일지

> 최종 정리: 2026-06-16
> 목적: 그동안 진행한 AI 관련 작업(모델 파인튜닝 → 서버 분리 → 되돌리기)을 한눈에 정리.
> "무엇을 했는지 / 하려다 못한 것 / 보류한 것 / 최종 상태"를 시간순으로 기록한다.
> 상세 구조 설명은 [AI_AGENT_GUIDE.md](AI_AGENT_GUIDE.md), 서버 실행은 [SERVER_GUIDE.md](SERVER_GUIDE.md) 참고.

---

## 0. 현재(최종) 상태 한 줄 요약

- **AI 서버는 단일 서버 구조(`model_server_qwen.py`, 포트 8000)로 운영**한다. (원래 구조)
- 한때 시도했던 **2-서버 분리(경량 챗봇 + Docker vLLM)는 되돌렸다.** 도커 컨테이너·이미지 삭제 완료.
- Java(`AIUtil`)는 처음부터 끝까지 `localhost:8000`만 호출 → **자바 코드 수정 0**.

---

## 1. 모델 파인튜닝 (튜닝) 이력

### 1-1. 무엇을 했나
사내 HR 문서(업무일지/회의록/출장보고서)를 **간결·구조적으로 요약**하도록 LLM을 LoRA로 파인튜닝했다.

| 항목 | 값 |
|---|---|
| 베이스 모델 | Qwen2.5-3B-Instruct (3.1B 파라미터) |
| 방식 | LoRA (SFT, TRL `SFTTrainer`) |
| 학습 스크립트 | `C:\Users\woojin\finetune_qwen.py` |
| 학습 데이터 | `hr_train_data_v5.json` (50개: 업무일지·회의록·출장보고서) |
| LoRA 설정 | r=16, alpha=32, dropout=0.05, target=q/k/v/o/gate/up/down_proj |
| 학습 파라미터 | 29,933,568개 (전체의 **0.96%**만 학습) |
| 양자화 | 4-bit NF4 (compute dtype bf16, double-quant) |
| 하이퍼파라미터 | 5 epoch / lr 2e-4 / cosine / batch 1 × grad-accum 8 / max_len 1024 |
| 옵티마이저 | paged_adamw_8bit (8GB VRAM 대응) |
| 결과 어댑터 | `C:\Users\woojin\qwen-finetuned\` (adapter 59.9MB) |

### 1-2. 학습 결과 (실측, `finetune_qwen.log`)
- train_loss: 1.41 → **0.90** (5 epoch)
- mean_token_accuracy: 0.70 → **0.853 (85.3%)**
- 학습 시간: **207.7초** (RTX 5060 Laptop 8GB)

### 1-3. 파인튜닝 변천사
| 모델 | 데이터 | 스크립트 | 토큰 정확도 | 비고 |
|---|---|---|---|---|
| EXAONE 2.4B | v1~v5 누적 | finetune_v7.py | 77.4% | 구버전(폐기) |
| **Qwen2.5-3B** | **v5 (50개)** | **finetune_qwen.py** | **85.3%** | **현재 사용** |

- 데이터셋도 v1 → v5로 반복 개선(`hr_train_data_v*.json`). 베이스 모델을 EXAONE에서 Qwen2.5-3B로 교체하며 정확도 77.4% → 85.3%로 향상.

---

## 2. A2A 챗봇 + 결정론 최적화 (완료)

> 상세: AI_AGENT_GUIDE.md 10·11장

### 2-1. 무엇을 했나
- 챗봇을 **A2A(Orchestrator + 6개 서브에이전트)** 구조로 설계.
- 핵심 최적화: **챗봇 경로에서 LLM을 완전히 제거**(결정론적 키워드 라우팅 + 각 에이전트가 DB 직접 조회).

### 2-2. 성과 (실측)
| 작업 | 최적화 전 | 최적화 후 | 개선 |
|---|---|---|---|
| 챗봇 응답 | 21초 | **0.26초** | 약 80배 |
| 문서 요약 | 70초 | **16.7초** | 약 4배 |
| 외부 API 비용 | — | **$0** | 온프레미스 |

### 2-3. 해결한 버그(요약)
환각(없는 이유 지어냄)·JSON 노출·`<tool_call>` 태그 노출·빈 답변·오라우팅·데이터 오염 등 10건. → **도구 결과를 재작성 없이 그대로 반환** + **출력 검증 가드(`looks_unnatural`)** 로 근본 해결. (상세 AI_AGENT_GUIDE 11장)

---

## 3. 2-서버 분리 작업 (시도 → 검증 완료 → **최종적으로 되돌림**)

> 배경: 챗봇은 LLM을 안 쓰는데, 오직 `/summarize` 하나 때문에 서버 켤 때마다 Qwen(4-bit)을 GPU에 올리느라 재시작 30초+. 그래서 챗봇과 요약을 물리적으로 분리하고 요약만 vLLM으로 가속해보려 함.

### 3-1. 한 것 (Part A/B/C 모두 동작 검증까지 성공)
- **Part A — 경량 챗봇 서버** `C:\Users\woojin\chat_server.py` 신규 생성.
  - torch/transformers/peft 등 모델 의존성 **전부 제거**, `/summarize`는 vLLM에 httpx 프록시.
  - import 0.64초, 기동 사실상 즉시, `/chat` 지연 0.025~0.15초. (기존 30초+ 대비 대폭 단축)
- **Part B — Docker vLLM 서버**(포트 8001) 가동.
  - **가장 큰 관문이던 RTX 5060 Blackwell sm_120 호환 → 통과.** vLLM 0.22.1 정상 구동.
  - VRAM 부족(8GB 중 ~1.1GB는 디스플레이 점유) 때문에 `--gpu-memory-utilization 0.80 --enforce-eager`로 안정화.
- **Part C — 연결·검증.** worklog 16.3s / meeting 12.6s / trip 12.9s 정상 요약 확인.

### 3-2. 솔직한 결론 (왜 되돌렸나)
- **단일 요청 속도는 기존(16.7초)과 거의 동일**(12~16초). bitsandbytes + `--enforce-eager` 조합이라 8GB·단일 요청에서는 vLLM의 핵심 가속(PagedAttention)이 크게 안 먹힘.
- vLLM의 진짜 이득은 **동시성(26배)** 과 **챗봇/GPU 분리(재시작 즉시)** 인데, 1인 시연·포트폴리오 환경에선 그 이득을 체감하기 어려움.
- **콜드 스타트 첫 요약 ~88초** 라는 운영 부담까지 감안 → 복잡도 대비 실익이 작다고 판단.
- **결정: 단일 서버(`model_server_qwen.py`)로 복귀.**

### 3-3. 되돌리기 실행 (2026-06-16)
- Docker Desktop 기동 → 컨테이너 `vllm-summary` 삭제 → 이미지 `vllm/vllm-openai:latest`(디스크 35GB / 콘텐츠 9.22GB) 삭제 → Docker·WSL 종료.
- `chat_server.py`는 **삭제하지 않고 보존**(향후 재시도 시 재사용 가능, 참고 산출물).
- 잔여: `nvidia/cuda:12.8.0-base`(402MB, 패스스루 테스트용 베이스 이미지)는 범용이라 보존.

---

## 4. 포트폴리오 산출물

- **아키텍처 다이어그램**: `portfolio_architecture.png` (생성 스크립트 `generate_diagram.py`).
  - 시스템 개요 + A2A 멀티에이전트 + 2-서버 구조 + 성과/기술스택을 한 장에 합본. 가로형(세로 길이 1/2로 축소 완료).
  - 참고: 다이어그램의 "②  2-서버 구조"는 위 3장에서 최종 되돌렸으므로, 포트폴리오에 쓸 때는 "시도·검증한 구조(현재는 단일 서버 운영)"로 설명하는 것이 정확함.

---

## 5. 하려다 못한 것 / 보류 / 미완

| 항목 | 상태 | 메모 |
|---|---|---|
| vLLM 단일 요청 속도 가속 | **미달성** | 12~16초로 기존과 동일. 8GB·bnb 한계 |
| CUDA 그래프 최적화(`--enforce-eager` 제거 + `--max-model-len 2048`) | **보류(미적용)** | 단일 20~30%↓ 기대되나 VRAM 빡빡·긴 문서 한도↓·OOM 위험. 8GB라 trade-off가 커서 안 함 |
| 더 작은 1.5B 모델로 요약 가속 | **미시도** | 속도 절반 가능하나 품질 저하 우려 |
| 2-서버 구조 운영 채택 | **철회** | 검증은 성공했으나 실익 작아 단일 서버로 복귀 |
| AI_AGENT_GUIDE.md 변경분 커밋 | **미커밋** | Part A/B/C 기록 추가분이 워킹트리에 남아 있음 |
| 모델 파인튜닝 과정 문서화 | **이번에 보완** | 기존 가이드에 학습 절차가 없어 본 작업일지 1장에 정리 |

---

## 6. 미커밋 / 워킹트리 변경 메모

- `AI_AGENT_GUIDE.md` (Part A/B/C 결과 + 운영 매뉴얼 추가) — **미커밋**
- `generate_diagram.py`, `portfolio_architecture.png` — 신규(미추적)
- `작업일지.md` (이 파일) — 신규
- 커밋 여부는 사용자 지시 대기.

---

## 7. 다시 빠르게 띄우려면 (현재 단일 서버 기준)

```powershell
# 1) MySQL
Get-Service MySQL80      # Running 아니면 Start-Service MySQL80

# 2) AI 모델 서버 (단일, 포트 8000, 기동 ~15~20초)
$env:PYTHONUTF8 = "1"
python C:\Users\woojin\model_server_qwen.py
#   상태: Invoke-RestMethod http://localhost:8000/health

# 3) Tomcat → http://localhost:8080/HRSystem/  (상세 SERVER_GUIDE.md)
```

> 2-서버(vLLM)를 다시 시도하고 싶으면: `chat_server.py`는 그대로 있으니, Docker 이미지(`vllm/vllm-openai:latest`)만 다시 받아서 AI_AGENT_GUIDE.md의 Part B 명령으로 띄우면 된다. (이미지 재다운로드 ~9GB 필요)
